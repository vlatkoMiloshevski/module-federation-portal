import { Routes } from '@angular/router';
import { AddonBundlesComponent } from './addonBundles.component';

export const ROUTES: Routes = [
    {
      path: 'addonBundles',
      component: AddonBundlesComponent
    }
];
